embedded.postgresql.dockerImage=postgres:11-alpine
embedded.postgresql.command="postgres -c 'max_connections=200'"
embedded.postgresql.database=test_db
embedded.postgresql.user=test_user
embedded.postgresql.password=test_password
